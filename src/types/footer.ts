export interface SocialLinkType {
  handleClick: () => void;
  Icon: React.FC;
}
export interface GuideListType { 
  title: string;
  handleClick: () => void;
}

 export  interface LanguagesType {
    code: string;
    name: string;
  }