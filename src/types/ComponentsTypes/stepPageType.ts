export interface StepPagePropsType {
  isAuth: boolean;
  stepNumber: string;
  stepTitle: string;
  contactText: string;
  organisation: boolean;
}
export interface StepNumberPropsType {
  stepNumber: string;
}
